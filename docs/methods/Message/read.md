# read()
This method is used to mark the message as read. The method requires the message number to be marked as read. The method will return `true` if the message is marked as read successfully, otherwise it will return `false`.

```php
$Message->read();
```
