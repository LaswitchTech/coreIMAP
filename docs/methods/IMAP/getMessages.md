# getMessages(string $folder = null, string $criteria = 'ALL', integer $limit = 0, boolean $reverse = false)
This method is used to get messages from the IMAP server.

```php
$IMAP->getMessages('INBOX', 'ALL', 0, false);
```
